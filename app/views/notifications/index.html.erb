<div class="about-image">
    <div class="container mt-3">
        <div class="text-center">
            <h1>通知一覧</h1>
        </div>
        <% if @notifications.size != 0 %>
            <%= render partial: 'notifications/notification', locals: {notification: @notifications} %>
        <% else %>
            <p>通知はございません</p>
        <% end %>
    </div> 
</div>
